[project]
name = "jujutsu-book"
version = "0.1.0"
description = "Livro sobre Jujutsu VCS compilado com Quarto"
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.0.0",
]

[tool.uv]
package = false